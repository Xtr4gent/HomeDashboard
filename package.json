{
  "name": "homedashboard-root",
  "private": true,
  "scripts": {
    "build": "npm --prefix web run build",
    "start": "npm --prefix web run start -- -H 0.0.0.0 -p ${PORT:-3000}",
    "dev": "npm --prefix web run dev"
  }
}
