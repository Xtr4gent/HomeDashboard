[build]
builder = "NIXPACKS"
buildCommand = "npm --prefix web ci && npm --prefix web run build"

[deploy]
startCommand = "npm --prefix web run start -- -H 0.0.0.0 -p $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
